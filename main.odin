package engine;

import "core:time"
import "core:fmt"

import gl "vendor:OpenGL"
import "vendor:glfw"

import "log"

window : glfw.WindowHandle;

main :: proc() {
    log.write("Starting");
    // glfw.Init();

    // window = glfw.GetWindow(800, 600, "Batch Renderer", nil, nil);

    // if window == nil {

    // }
}